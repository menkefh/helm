apiVersion: v1
kind: Service
metadata:
  name: {{ include "elasticcchmc.fullname" . }}
  labels:
    {{- include "elasticcchmc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "elasticcchmc.selectorLabels" . | nindent 4 }}
