apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "graylogcchmc.fullname" . }}-test-connection"
  labels:
    {{- include "graylogcchmc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "graylogcchmc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
