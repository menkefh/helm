apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongodbcchmc.fullname" . }}-test-connection"
  labels:
    {{- include "mongodbcchmc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongodbcchmc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
