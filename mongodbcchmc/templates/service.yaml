apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongodbcchmc.fullname" . }}
  labels:
    {{- include "mongodbcchmc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongodbcchmc.selectorLabels" . | nindent 4 }}
